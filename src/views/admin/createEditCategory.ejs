<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%- title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/admin/admin-theme.css">
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">ShopMaster</a>
    </div>
  </nav>

  <!-- Admin Panel -->
  <div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1><%- title %></h1>
      <button class="btn btn-secondary" onclick="goToCategories()">
        ← Volver
      </button>
    </div>

    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card shadow">
          <div class="card-header bg-primary text-white">
            <h4 class="mb-0"><%= title.includes('Create') ? 'Crear Categoría' : 'Editar Categoría' %></h4>
          </div>
          <div class="card-body">
            <form id="categoryForm">
              <div class="mb-3">
                <label for="categoryName" class="form-label">Nombre *</label>
                <input type="text" class="form-control" id="categoryName" required>
                <div class="invalid-feedback">
                  El nombre es requerido
                </div>
              </div>

              <div class="mb-3">
                <label for="categoryImage1" class="form-label">Imagen 1</label>
                <input type="file" name="images" class="form-control" id="categoryImage1" accept="image/*">
                <div class="form-text">
                  Soporta imágenes JPG, PNG, GIF. Máximo 2MB.
                </div>
                <div id="imagePreview1" class="mt-2" style="display: none;">
                  <img id="imagePreviewImg1" src="" alt="Vista previa" style="max-height: 200px;">
                </div>
              </div>

              <div class="mb-3">
                <label for="categoryDescription" class="form-label">Descripción</label>
                <textarea class="form-control" id="categoryDescription" rows="3"></textarea>
              </div>

              <div class="mb-3">
                <div class="form-check form-switch">
                  <input class="form-check-input" type="checkbox" id="showToClients" checked>
                  <label class="form-check-label" for="showToClients">
                    Mostrar categoría en el sitio web
                  </label>
                </div>
              </div>

              <div class="d-grid gap-2">
                <button type="submit" class="btn btn-primary">
                  <%= title.includes('Create') ? 'Crear Categoría' : 'Actualizar Categoría' %>
                </button>
                <button onclick="goToCategories()" class="btn btn-secondary">
                  ← Volver
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Success Modal -->
  <div class="modal fade" id="successModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content rounded-3">
        <div class="modal-header">
          <h5 class="modal-title text-success">¡Éxito!</h5>
        </div>
        <div class="modal-body">
          <p id="successMessage" class="text-center">Categoría creada exitosamente</p>
        </div>
        <div class="modal-footer justify-content-center">
          <button id="backBtn" class="btn btn-secondary">Ir a categorías</button>
          <button id="continueBtn" class="btn btn-primary">Crear otra categoría</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-dark text-white py-4 mt-5">
    <div class="container text-center">
      <p class="mb-0">&copy; 2026 ShopMaster. Panel de Administración.</p>
    </div>
  </footer>

  <script>
    window.createEditCategoryData = {
      title: <%- JSON.stringify(title) %>,
      category: <%- JSON.stringify(category || null) %>,
    };

    function goToCategories() {
      window.location.href = '/admin/categories';
    }
  </script>
  <script src="/js/createEditCategory/createEditCategory.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
